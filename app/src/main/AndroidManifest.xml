<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aliyayman.sozluk4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dic_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sozluk4">
        <activity android:name=".activity.QuizeActivity" />
        <activity android:name=".activity.ResultActivity" />
        <activity android:name=".activity.QuizeAnaActivity" />
        <activity android:name=".activity.KelimeActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>